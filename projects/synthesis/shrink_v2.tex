\section{Shrinking}


\inb{ consider changing the name to something like hight lines / contour in order to hint on the similarity to topography map. In addition, the embedding inside $\mathbb{R}^{d}$ should appears before that definition. }
\begin{definition}[Potential Walls]
  We define the potential walls to be a functions collection from the vertices of $\mathcal{G}_{n}^{d}$ to the reals as follow:
  \begin{enumerate}
    \item For any coordinate $i \in [d]$, we define $L_{i} \colon \mathcal{G}_{n}^{d} \rightarrow \mathbb{R}$ to act as $L_{i}(v) = v_{i}$.
    \item For any subset of coordinates $I \subset [d]$ we define $L_{I} \colon  \mathcal{G}_{n}^{d} \rightarrow \mathbb{R}$ to act as $L_{I}(v) = -\sum_{i \in I}v_{i}$. 
  \end{enumerate}
For a subset of vertices $\Gamma \subset \mathcal{G}_{n}^{d}$, a vertex $v \in \Gamma$, and a potential wall $l \colon \mathcal{G}_{n}^{d} \rightarrow \mathbb{R}$, we say that $v$ is a \textit{local maximum} of $l$ in $\Gamma$ if for any neighbor $u$ of $v$, such that $u \in \Gamma$, we have $l(v) \ge l(u)$. We say that $v$ is a strong local maximum if the inequality is strict, namely $l(v) > l(u)$. When the subset $\Gamma$ is clear from the context, we might omit it and briefly say that $v$ is a local maximum of $l$.

\end{definition}

\inb{
  What we want is that if a bit contains both $v, gv$ then there will be a check that contains both $v, gv$, that is true if $d^{\prime}  > 1$, then it means that we can earse from the bit a vertex which it is not $gv$. 

}



\begin{claim}
  \label{claim:localmax}
Let $z$ be an assignment, and let $\partial z$ be the set of vertices adjacent to unsatisfied checks induced by $z$. Let $v$ be a vertex in $\partial z$, and let $g$ be a coordinate in $S$ such that $v$ is a local maximum of $L_{g}$. Then $gv \notin \partial z$.
\end{claim}
\begin{proof}
  Suppose $gv \in \partial z$, then 
  \begin{equation*}
    \begin{split}
      L_{g}(gv) = (gv)_{g} = 1 + v_{g} = 1 + L_{g}(v)
    \end{split}
  \end{equation*}
In contradiction to $v$ being a local maximum of $L_{g}$ in $\partial z$.
\end{proof}
\begin{claim}
  \label{claim:edge_pole}
Let $z$ be an assignment, and let $\partial z$ be the set of vertices adjacent to unsatisfied checks induced by $z$. Let $v$ be a vertex in $\partial z$, and let $g$ be a coordinate in $S$ such that $v$ is a local maximum of $L_{g}$. Then for any check associated with a $d^{\prime}-1$ face rooted at $v$ that contains $gv$, it is satisfied.
\end{claim}
\begin{proof}
Assume, through contradiction, the existence of an unsatisfied check associated with a $d^{\prime}-1$ positive face rooted at $v$, which contains $gv$. This implies $gv \in \partial z$, contradicting \Cref{claim:localmax}.
\end{proof}


\begin{definition}[Small Code at Vertex $v$]
For a vertex $v$, we define the \textit{$X$-small code at vertex $v$} to be the binary assignments over the bits ($d^{\prime}$ faces) rooted at $v$ that satisfy $\mathcal{C}_{X}$'s restrictions rooted at $v$ ($d^{\prime}-1$ faces). Similarly, we define the \textit{$Z$-small code at vertex $v$} to be the binary assignments over the bits ($d^{\prime}$ faces) rooted at $v$ that satisfy $\mathcal{C}_{Z}$'s restrictions rooted at $v$ ($d^{\prime}+1$ faces). We denote by $\mathcal{C}_{v}^{-}$ and $\mathcal{C}_{v}^{+}$ the $X$-small code at $v$ and $Z$-small code at $v$.
\end{definition}

\begin{definition}
  \inb{Should rewrite it better.} We define $\text{Stab}_{v}^{-}$ and $\text{Stab}_{v}^{+}$ as the following linear codes: 
  \begin{align*}
    \text{Stab}_{v}^{-} \colonequals  \bigl\{ z : \partial z  &= 0 \text{ and } \\
      & z \in \text{Span} \left( \left\{ \rotfv{v}{S^{\prime}/g}, \rotfv{gv}{S^{\prime}/g} \text{ for } S^{\prime} \subset S , |S^{\prime}|=d^{\prime}+1, g \in S^{\prime} \right\} \right) \bigr\}
  \end{align*}
  \begin{align*}
    \text{Stab}_{v}^{+} \colonequals  \bigl\{ z : \partial^{*} z  &= 0 \text{ and } \\
      & z \in \text{Span} \left( \left\{ \rotfv{v}{S^{\prime}/g}, \rotfv{gv}{S^{\prime}/g} \text{ for } S^{\prime} \subset S , |S^{\prime}|=d^{\prime}+1, g \in S^{\prime} \right\} \right) \bigr\}
  \end{align*}
\end{definition}

\ctt{
  Regarding the beneath, The number of restriction in $\partial^{+}$ case was counted in negligently. In addition, we should show that for the $\partial^{-}$ case, restrictions of the form $\rotfv{g_{2}g_{1}v}{S}$ are degenerate. In the bottom line it's look like:  
  \begin{equation*}
    \begin{split}
      \partial^{+} \rotfv{g_{2}g_{1}v}{S} = \partial^{+} \rotfv{g_{1}v}{(S/ g_{3}) \cup g_{2} } +  \partial^{+} \rotfv{g_{2}v}{(S / g_{3}) \cup g_{1}} 
    \end{split}
  \end{equation*}
  Lets develop it, consider a restriction rooted at $g_{2}g_{1}v$, and let's look on it's boundary:
  \begin{equation*}
    \begin{split}
      \partial^{+} \rotfv{g_{2}g_{1}v}{S^{\prime}} &=  \sum_{g\in S/S^{\prime}} \rotfv{g_{2}g_{1}v}{S^{\prime} \cup g } +  \sum_{g\in S/S^{\prime}} \rotfv{g^{-1}g_{2}g_{1}v}{S^{\prime} \cup g }  
    \end{split}
  \end{equation*}
  Now, any term in the left closure is a bit beyond the scope of the bits in $\text{Stab}_{v}^{-}$, thus, we are zeroing them out. The bits, on the right closers are in the scope, only if $g \in \{g_{1}, g_{2}$. Thus, in total the check is equivalence to: 
  \begin{equation*}
    \begin{split}
      \partial^{+} \rotfv{g_{2}g_{1}v}{S^{\prime}} &=  \rotfv{g_{1}v}{S^{\prime} \cup g_{2} } \cdot \mathbf{1}_{g_{2} \notin S^{\prime}} \\
      & +\rotfv{g_{2}v}{S^{\prime} \cup g_{1} } \cdot \mathbf{1}_{g_{1} \notin S^{\prime}} 
    \end{split}
  \end{equation*}
  \begin{equation*}
    \begin{split}
      \partial^{+} \rotfv{g_{1}v}{(S^{\prime}/ g_{3}) \cup g_{2} } &= \sum_{g\in S/S^{\prime}} \rotfv{g_{1}v}{(S^{\prime}/ g_{3}) \cup g_{2}   \cup g } +  \sum_{g\in S/S^{\prime}} \rotfv{g^{-1}g_{1}v}{(S^{\prime}/ g_{3}) \cup g_{2}   \cup g }  \\
      \partial^{+} \rotfv{g_{2}v}{(S / g_{3}) \cup g_{1}} &= \sum_{g\in S/S^{\prime}} \rotfv{g_{2}v}{(S^{\prime}/ g_{3}) \cup g_{1}   \cup g } +  \sum_{g\in S/S^{\prime}} \rotfv{g^{-1}g_{2}v}{(S^{\prime}/ g_{3}) \cup g_{1}   \cup g }  \\
    \end{split}
  \end{equation*}
  \begin{equation*}
    \begin{split}
      \partial^{+} \rotfv{g_{1}v}{(S^{\prime}/ g_{3}) \cup g_{2} } &= \left(\sum_{g\in S/S^{\prime}} \rotfv{g_{1}v}{(S^{\prime}/ g_{3}) \cup g_{2}   \cup g } \right) +  \rotfv{v}{(S^{\prime}/ g_{3}) \cup \{ g_{2}, g_{1} \} }  \\
    \partial^{+} \rotfv{g_{2}v}{(S / g_{3}) \cup g_{1}} &= \left( \sum_{g\in S/S^{\prime}} \rotfv{g_{2}v}{(S^{\prime}/ g_{3}) \cup g_{1}   \cup g } \right) + \rotfv{v}{(S^{\prime}/ g_{3}) \cup \{g_{1},g_{2}\} }  \\
    \end{split}
  \end{equation*}
  Other way: 
  \begin{equation*}
    \begin{split}
      \partial^{+} \sum_{g^{\prime} \in S/S^{\prime}}\rotfv{g^{\prime}v}{S^{\prime}} &= \sum_{g^{\prime} \neq g \in S/S^{\prime}}\rotfv{g^{\prime}v}{S^{\prime}\cup g} + \sum_{g \in S/S^{\prime}}\rotfv{v}{S^{\prime} \cup g} \\
      &= \sum_{g^{\prime} \neq g \in S/S^{\prime}}\rotfv{g^{\prime}v}{S\cup g} + \partial^{+}\rotfv{v}{S^{\prime}} 
    \end{split}
  \end{equation*}
  Where we use the fact that if $g = g^{\prime}$ then the bit is beyond the scope. In addition: 
  \begin{equation*}
    \begin{split}
      \partial^{+} \sum_{g^{\prime} < g \in S/S^{\prime}}\rotfv{g^{\prime}gv}{S^{\prime}} &=\sum_{g^{\prime} < g \in S/S^{\prime}}\rotfv{g^{\prime}v}{S^{\prime} \cup g} + \rotfv{gv}{S^{\prime} \cup g^{\prime}} \\
      &= \sum_{g^{\prime} \neq g \in S/S^{\prime}}\rotfv{g^{\prime}v}{S^{\prime} \cup g} \\
      &=  \partial^{+} \sum_{g^{\prime} \in S/S^{\prime}}\rotfv{g^{\prime}v}{S^{\prime}} + \partial^{+}\rotfv{v}{S^{\prime}} 
    \end{split}
  \end{equation*}
  Thus, we find more ${ d \choose d^{\prime} - 1 }$ restriction over the checks. [We need to show that those restrictions are independent]. In addition:
  \begin{equation*}
    \begin{split}
      \partial^{+} \sum_{S^{\prime}}\sum_{g^{\prime} < g \in S/S^{\prime}}\rotfv{g^{\prime}gv}{S^{\prime}} &= 0
    \end{split}
  \end{equation*}
  \begin{equation*}
    \begin{split}
      \partial^{+} \rotfv{g_{2}g_{1}v}{S} = \partial^{+} \rotfv{g_{1}v}{(S/ g_{3}) \cup g_{2} } +  \partial^{+} \rotfv{g_{2}v}{(S / g_{3}) \cup g_{1}} 
    \end{split}
  \end{equation*}
  I don't succes to advance the idea more than that, So in the end I believe that I will do is to say that the diminsion of $\text{Stab}_{v}^{-}$ is at least ${ d \choose d^{\prime}+ 1 } $. And by fixing coordinates we add more ${ d \choose d^{\prime} } $ restriction. So the dimension becomes: 
  \begin{equation*}
    \begin{split}
     & { d \choose d^{\prime}+1 } - { d \choose d^{\prime} } \\
    \end{split}
  \end{equation*}
  \begin{equation*}
    \begin{split}
      & { d \choose d^{\prime}+1 } - { d \choose d^{\prime} } + { d \choose d^{\prime} - 1   } \\
    \end{split}
  \end{equation*}
  Note that since we talk on mapping checks $\rightarrow$ bits, we use the $\partial^{+}$. Eventually, we need to use the poincare duality $H^{d^{\prime}} \rightarrow H_{d - d^{\prime}}$, the duality also should be used to define the SWEEP rule to $C_{Z}$. I didn't successes to do it other way. 
}

\begin{claim}
  \label{claim:agreement}
  Let $v$ be a vertex. For any $z \in \mathcal{C}_{v}^{\pm}$, there is a stabilizer $w \in \text{Stab}_{v}^{\pm}$ such that $z = w|_{v}$; namely, $z$ and $w$ agree on the faces rooted at $v$.
\end{claim}
\begin{proof}
  We will show that codes obtained from $\text{Stab}_{v}^{\pm}$ by forcing the faces rooted at $v$ to $z|_{v}$ have non-zero dimensions. Then it follows that any code word of $\mathcal{C}_{v}^{\pm}$ could be extend to codewords in $\text{Stab}_{v}^{\pm}$. The number of bits in both $\text{Stab}_{v}^{\pm}$ is:
\begin{equation*}
  \begin{split}
      { d \choose d^{\prime} }  +  d { d - 1 \choose d^{\prime} } 
  \end{split}
\end{equation*}
The numbers of checks for $\text{Stab}_{\pm}$ are:
\begin{equation*}
  \begin{split}
 { d \choose d^{\prime} \pm 1 } + d { d-1  \choose d^{\prime} \pm 1 }  
 \end{split}
\end{equation*}
The restriction to $z|_{v}$ sets the bits rooted at $v$ and satisfies the checks that are rooted at $v$, so the checks of the form $\rotfvs{}$ for $|S^{\prime}| = d^{\prime} \pm 1$ are degenerate, and the dimension that remains is:
\begin{equation*}
  \begin{split}
    \dim \ge d { d - 1 \choose d^{\prime} } - d { d-1  \choose d^{\prime} \pm 1 }  
  \end{split}
\end{equation*}
In the case that $d^\prime = (d-1)/2$, then for any $x \in [d-1]$ we have $ { d - 1 \choose d^{\prime} } >  { d-1 \choose x  }  $ therefore:
\begin{equation*}
  \begin{split}
    \dim &\ge d { d - 1 \choose d/2 } - d { d-1  \choose d/2 \pm 1 } \\
    & > d
  \end{split}
\end{equation*}
So there is at least $2^{d}$ stabilizers $w \in \text{Stab}_{v}$ that agree with $z$. 
\end{proof}

\begin{claim}
  \label{claim:reduce_stab}
  Let $z$ be a finite assignment. Suppose that 
  \begin{equation*}
    \begin{split}
            \max \big\{ L_{d+1}(u) :  u \in z \big\} > \max \big\{ L_{d+1}(u) : u \in \partial^{\pm} z \big\}
    \end{split}
  \end{equation*}
  Then there is $\tilde{z} \sim_{\mathcal{C}_{X}} z$ such that:
\begin{equation*}
  \begin{split}
    \max \left\{ L_{d+1}(u) : u \in \tilde{z} \right\} & \le  \max \big\{ L_{d+1}(u) : u \in z \big\} - 1
  \end{split}
\end{equation*}
\end{claim}
\begin{proof}
Let $v_{1}, v_{2}, \ldots, v_{k}$ be the vertices in $z$ that achieve the maximum for $L_{d+1}$, namely $v_{1}, v_{2}, \ldots, v_{k} = \arg \max_{v \in z} L_{d+1}(v)$, and assume that $v_{i} \notin \partial^{\pm} z$ for any $i \in [k]$.

Consider the $i$th vertex $v_{i}$. By definition, since $v_{i} \notin \partial^{\pm} z$, we have $\partial^{\pm} z|_{v_{i}} = 0$, and since $v_{i}$ is a maximum point of $L_{d+1}$ in $z$, it is also a local maximum, and therefore $z|_{v_{i}+} = z|_{v_{i}}$. 
Combining the two, we get that $z|_{v_{i}}$ is a codeword of the small code at $v_{i}$, namely $z|_{v_{i}} \in \mathcal{C}_{v_{i}}^{\pm}$. Thus, by \Cref{claim:agreement}, there is a stabilizer $w_{i} \in \text{Stab}_{v_{i}}^{\pm}$ such that $w_{i}$ and $z$ agree on the positive faces of $v_{i}$, namely $w_{i}|_{v_{i}} = z|_{v_{i}}$. 

Set $\tilde{z} \leftarrow z + \sum_{i}w_{i}$. Since $\sum_{i}w_{i}$ is a stabilizer, we have $\tilde{z} \sim_{C_{X}} z$. In addition, for any $j \neq i$, it follows that $v_{j} \notin w_{i}$.
Otherwise, there exists $S^{\prime} \subset S$ such that $v_{j} \in \rotf{v_{i}}{S^{\prime}}$, and therefore $L_{d+1}(v_{j}) > L_{d+1}(v_{i})$, in contradiction to $v_{i}$ and $v_{j}$ being both maximum points for $L_{d+1}$ in $\partial^{\pm} z$. 
Thus, we have:
\begin{equation*}
  \begin{split}
    \tilde{z}|_{v_{i}} = (z + \sum_{i}w_{i})|_{v_{i}} + (z + w_{i})|_{v_{i}} = 0
  \end{split}
\end{equation*}
Namely, for any $i$, the $i$th vertex $v_{i}$ is not in $\tilde{z}$. Hence, the vertex domain of $\tilde{z}$ is contained in the union of the vertices of $z$ with the vertices of $w_{1}, w_{2}, \ldots, w_{k}$, substituting $v_{1}, v_{2}, \ldots, v_{k}$. Overall, we get:
\begin{equation*}
  \begin{split}
  \max \left\{ L_{d+1}(u) : u \in \tilde{z} \right\} & \le \max \big\{ L_{d+1}(u) : u \in z \bigcup_{i} w_{i} \big/ \bigcup_{i} v_{i} \big\}  \\ 
     & \le  \max \big\{ L_{d+1}(u) : u \in z \big\} - 1
  \end{split}
\end{equation*}
\end{proof}

\begin{claim}
  \label{claim:extermum}
  Let $z$ be a finite assignment. Then there is $\tilde{z} \sim_{\mathcal{C}_{X}} z$ such that 
  \begin{equation*}
    \begin{split}
      \max \big\{ L_{d+1}(u) :  u \in \tilde{z} \big\} = \max \big\{ L_{d+1}(u) : u \in \partial^{\pm} z \big\}
    \end{split}
  \end{equation*}
\end{claim}
\begin{proof}
Let $z$ be a finite assignment, and let $k$ be an integer such that $k \ge 1$ and
  \begin{equation*}
    \begin{split}
            \max \big\{ L_{d+1}(u) :  u \in z \big\} =  \max \big\{ L_{d+1}(u) : u \in \partial^{\pm} z \big\} + k
    \end{split}
  \end{equation*}
  For an integer $l \le k$, consider the assignments $\tilde{z}_{0},\tilde{z}_{1},..,\tilde{z}_{l}$ defined as follow: 
  \begin{itemize}
    \item The first element, $\tilde{z}_{0} = z$. 
    \item If the $i-1$th element satisfies 
  \begin{equation*}
    \begin{split}
      (\star ) \ \ \max \big\{ L_{d+1}(u) :  u \in \tilde{z}_{i-1} \big\} > \max \big\{ L_{d+1}(u) : u \in \partial^{\pm} \tilde{z}_{i-1} \big\} 
    \end{split}
  \end{equation*}
      Then define the $i$th element, $\tilde{z}_{i}$, to be the result of applying the construction, given in \Cref{claim:reduce_stab}, on $\tilde{z}_{i-1}$ , namely  $\partial \tilde{z}_{i+1} = \partial z_{i}$ and  
\begin{equation*}
  \begin{split}
    \max \big\{ L_{d+1}(u) :  u \in \tilde{z}_{i} \big\} \le  \max \big\{ L_{d+1}(u) : u \in \tilde{z}_{i-1} \big\} - 1
  \end{split}
\end{equation*}
If the $i-1$th element doesn't satisfies $\star$, then set $l = i -1$.
\item If the $k$th element was defined, set $l = k$.
  \end{itemize}
Since $\sim_{C_{X}}$ is a transitive relation, we have that for any $i \in [l]$, $z \sim_{C_{X}} \tilde{z}_{i}$. If $l \neq k$, then there exists $\tilde{z}_{i}$ such that $\tilde{z}_{i} \sim_{C_{X}}$ and $\tilde{z}_{i}$ satisfies $\star$, namely by setting $\tilde{z} = \tilde{z}_{i}$, we get the desired.
So, it is left to handle the case where $l = k$. In that case, set $\tilde{z} = \tilde{z}_{k}$. By \Cref{claim:reduce_stab}:
\begin{equation*}
  \begin{split}
    \max \big\{ L_{d+1}(u) :  u \in \tilde{z} \big\} \le  \max \big\{ L_{d+1}(u) : u \in \partial^{\pm} z \big\}
  \end{split}
\end{equation*}
Yet, since $\tilde{z} \sim_{C_{X}} z$, it follows that $\partial \tilde{z} = \partial z$, So: 
\begin{equation*}
  \begin{split}
     \max \big\{ L_{d+1}(u) :  u \in \partial^{\pm} z \big\} = \max \big\{ L_{d+1}(u) :  u \in  \partial^{\pm} \tilde{z} \big\} \le \max \big\{ L_{d+1}(u) : u \in z \big\} 
  \end{split}
\end{equation*}
Therefore, we have the equality:
\begin{equation*}
  \begin{split}
    \max \big\{ L_{d+1}(u) :  u \in \tilde{z} \big\} = \max \big\{ L_{d+1}(u) : u \in \partial^{\pm} z \big\}
  \end{split}
\end{equation*}
\end{proof}

\begin{claim}
Let $z$ be a finite assignment and let $\xi$ be the result of the application of the Sweep rule on it. Then: 
  \begin{enumerate}
    \item For any $i \in [d]$: $\max \big\{ L_{i}(v) : v \in \partial z \big\} = \max \big\{ L_{i}(v) : v \in \partial \xi \big\}  $
    \item And for $i = d+1$ : $ \max \big\{ L_{d+1}(v)  : v \in \partial z \big\} > \max \big\{  L_{d+1}(v) : v \in \partial \xi \big\} + 1$
  \end{enumerate}
\end{claim}
\begin{proof}
First, since the Sweep rule is invariant to addition by stabilizers, for any $\tilde{z}$ such that $\tilde{z} \sim_{C_{X}} z$, the correction applied by the Sweep rule on $\tilde{z}$ is the same as the application on $z$. Denote by $\phi$ the correction, by $\tilde{\xi}$ the result of applying the Sweep rule on $\tilde{z}$, and by $w$ the stabilizer which is the difference between $z$ and $\tilde{z}$, namely:
  \begin{equation*}
    \begin{split}
      \xi &= z + \phi \\
      \tilde{\xi} &= \tilde{z} + \phi \\
      \tilde{z} &= z + w
    \end{split}
  \end{equation*}
  The $\partial^{\pm}$ boundary of $\tilde{\xi}$ equals:
  \begin{equation*}
    \begin{split}
      \partial \tilde{\xi} = \partial \left( \tilde{z} + \phi \right) = \partial \left( z + \phi \right) = \partial \xi
    \end{split}
  \end{equation*}
  So, in total, we have that for any $\tilde{z}$ such that $\tilde{z} \sim_{C_{X}} z$, it holds that $\partial \tilde{z} = \partial z$ and $\partial \tilde{\xi} = \partial \xi$. Therefore, showing that there exists $\tilde{z} \sim_{C_{X}} z$ for which the claim holds proves the claim for $z$. By \Cref{claim:extermum}, there is $\tilde{z} \sim_{C_{X}} z$ such that:
  \begin{equation*}
    \begin{split}
      \max \big\{ L_{d+1}(u) :  u \in \tilde{z} \big\} = \max \big\{ L_{d+1}(u) : u \in \partial^{\pm} \tilde{z} \big\}
    \end{split}
  \end{equation*}
  
  For such $\tilde{z}$, any maximum of $L_{d+1}$ in $ \partial \tilde{z} $, is also a maximum in $\tilde{z}$. Denote by $v_{1},..,v_{k}$ the maximum points in $\partial \tilde{z}$. Since they are maximum points in $\tilde{z}$, it holds that for each $i \in [k]$, $\tilde{z}|_{v_{i}} = \tilde{z}|_{v_{i}+}$, and hence $\partial \tilde{z}|_{v_{i}} = \partial \tilde{z}|_{v_{i}+}$. Since $\tilde{z}|_{v_{i}+}$ is an assignment rooted at $v_{i}$, then $v_{i}$ can suggest $\tilde{z}|_{v_{i} +}$, namely the condtion in Sweep rule is satisfied for vertex $v_{i}$. Thus:
\begin{equation*}
  \begin{split}
\partial\tilde{\xi}|_{v_{i}} =  \partial\tilde{z}|_{v_{i}} + \partial\phi|_{v_{i}} = 0
  \end{split}
\end{equation*}
Namely, for any $i$, the $i$th vertex $v_{i}$ is not in $\tilde{\xi}$. For any vertex $v$ denote by $\phi_{v}$ the correction made by the vertex $v$, namely $\phi = \sum_{v \in \partial \tilde{z}} \phi_{v}$. Hence, the vertex domain of $\tilde{\xi}$ is contained in the union of the vertices of $\tilde{z}$ with the vertices of $ \cup_{v \in V} \phi_{v} $, substituting maximum points $v_{1}, v_{2}, \ldots, v_{k}$. Overall, we get:
\begin{equation*}
  \begin{split}
    \max \left\{ L_{d+1}(u) : u \in \partial \tilde{\xi} \right\} & \le \max \big\{ L_{d+1}(u) : u \in  \partial \tilde{z} \bigcup_{v \in \partial \tilde{z} } \phi_{v} \big/ \bigcup_{i} v_{i} \big\}  \\ 
    & \le  \max \big\{ L_{d+1}(u) : u \in \partial \tilde{z} \big\} - 1
  \end{split}
\end{equation*}
Let $v$ be a vertex that is a maximum of $L_{g}$ in $\partial \tilde{z}$ , maximum points of $L_{g}$ do not see syndrome at the $g$ direction. If  and: 
\begin{equation*}
  \begin{split}
    \phi_{v} &= \sum \rotfv{v}{S^{\prime}_{i}} 
  \end{split}
\end{equation*}
By \Cref{claim:edge_pole} $\partial \tilde{z}|_{gv} = 0 $, Hence: 
\begin{equation*}
  \begin{split}
    \partial \phi_{v}|_{gv} &= \left( \partial \sum \rotfv{v}{S^{\prime}_{i}} \right)|_{gv} \\
    &=  \left(  \sum_{S^{\prime}_{i}} \sum_{g^{\prime} \in S^{\prime}_{i} }\rotfv{v}{S^{\prime}_{i}/g^{\prime}} +\rotfv{g^{\prime}v}{S^{\prime}_{i}/g^{\prime}} \right)|_{gv} \\ 
  & = 0 
  \end{split}
\end{equation*}
Thus if  $g  \in \cup S^{\prime}_{i}$, Then: 
\begin{equation*}
  \begin{split}
    \rotfv{gv}{S^{\prime}_{i}/g}  \in \partial \phi_{v}|_{gv} 
  \end{split}
\end{equation*}
So for $\rotfv{gv}{S^{\prime}_{i}/g} = 0$, it must appear at least twice in the sum, yet the left terms in the sum cannot contain it (they differ by the root vertex), and if there is a right term $\rotfv{gv}{S^{\prime}_{j}/g} = \rotfv{gv}{S^{\prime}_{i}/g}$, then it implies that $S_{j} = S_{i}$. Which is a contradiction, therefore: $g \notin \cup S^{\prime}_{i}$ and $gv \notin \phi_{v}$. 
\begin{equation*}
  \begin{split}
    \max \left\{ L_{g}(u) : u \in \partial \tilde{\xi} \right\} & \le \max \big\{ L_{g}(u) : u \in  \partial \tilde{z} \bigcup_{v \in \partial \tilde{z} } \phi_{v} \big/ \bigcup_{i} v_{i} \big\}  \\ 
    & \le  \max \big\{ L_{g}(u) : u \in \partial \tilde{z} \big\} + 1 - 1
  \end{split}
\end{equation*}
\end{proof}



