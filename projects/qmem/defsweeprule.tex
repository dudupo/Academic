



\subsection{Sweep Rule}

The sweep-rule is the core of our construction. A decoding cycle begins with each of the vertices reading the syndrome on its positive $X$-checks (checks that are associated with the positive $d^{\prime}-1$ dimensional faces of the vertex) and, based on what it sees, applying a Pauli correction. The sweep-rule defines the correction to be over the positive qubits such that it will turn off the positive syndrome. It's not hard to see that for the $(2,2)$-Toric complex, the Toom's and the sweep rules are the same. Compared to other correction cycles on topological codes, the main advantage of the sweep is that it is independent of time. The invention of the sweep-rule is attributed to Aleksander Kubica and John Preskill \cite{Kubica_2019}. Here we extend their work to cubical complexes.

\begin{definition}[Sweep Rule]
  \label{def:sweeprule}
  Let $z$ be an assignment on the $d^{\prime}$-faces. The \textit{sweep rule decoding procedure} at a vertex $v \in \mathcal{G}_{n}^{d}$ is defined as follows:
  
  Measure the checks rooted at $v$ and look for a rooted assignment $\tilde{z}$ at $v$ such that: 
  \begin{enumerate}
    \item $ \mathbf{ln}_{0}(\tilde{z}) \subset \mathbf{ln}_{0}(\partial^{-}z|_{v+})$ % . Any $d^{\prime}$ dimensional face $\rotfvs{}$ in $\tilde{z}$, satisfies $ (\partial^{-})^{d^{\prime}}\rotfvs{} \subset  \partial^{-}(z)|_{v+}$. 
    \item And $(\partial^{-}\tilde{z})|_{v+} = \partial^{-}(z)|_{v+}$.
  \end{enumerate}
  In other words the $X$-checks rooted at $v$ have the same syndromes for $z$ and $\tilde{z}$. If such an assignment exists, flip $\tilde{z}$. \inb{Write again. Consider the update}. 
\end{definition}

\begin{claim}
  For any nontrivial boundary on the positive checks at vertex $v$ the sweep rule admits a suggestion. 
\end{claim}
\begin{proof} 
  Denote by $\phi$ the correction applied by vertex $v$ so $\partial^{-} \phi|_{v+} = \partial^{-} z |_{v+}$. We will show that if the support property is not hold then there is another..   Consider the collection of faces that form $\partial^{-} \phi |_{v}$:
\begin{equation*}
  \begin{split}
    \partial^{-} \phi |_{v} &= \sum_{S^{\prime}_{i}} \rotfv{v}{S^{\prime}_{i}} 
  \end{split}
\end{equation*}
Now, imagine the extansion of $\phi$ to $\phi^{*}$ such any face in $\phi^{*}$ is obtained by taking a face of $\phi$ adding to it a imaginary direction $e$. Therefore:
\begin{equation*}
  \begin{split}
    \partial \phi^{*} |_{e} &= \sum_{S^{\prime}_{i}} \rotfv{v}{S^{\prime}_{i} \cup e} 
  \end{split}
\end{equation*}
% Since $\phi$ is a rooted at vertex $v$, we have that $\partial \phi |_{v+}$ is also rooted at vertex $v$.
Using the zero boundary of boundary $(\partial^{-})^{2}\phi^{*} = 0$ we have that:
\begin{equation*}
  \begin{split}
    (\partial^{-})^{2}\phi^{*} |_{e} =   \partial^{-} \left( \sum_{S^{\prime}_{i}} \rotfv{v}{S^{\prime}_{i} \cup e} \right) |_{e}  = \sum_{S^{\prime}_{i}}\sum_{g^{\prime} \in S^{\prime}_{i}} \rotfv{v}{S^{\prime}_{i}\cup e /g} = 0   
  \end{split}
\end{equation*}
For $d^{\prime}=2$, any of the subsets $S^{\prime}_{i}\cup e/g^{\prime}$ is the singleton $\{e\}$, thus for the sum zeroing out, the number of faces in $\partial^{*}\phi$ has to be even, and therefore also in $\partial \phi$. 

Thus, one can find a perfect matching between the $M : \{ S^{\prime}_{i} \} \rightarrow \{ S^{\prime}_{i} \}$, let $A$ be a subset of $\{ S^{\prime}_{i} \} $ such $M : A \rightarrow \bar{A}$. Defines: 
\begin{equation*}
  \begin{split}
    \tilde{z} \colonequals \sum_{S^{\prime}_{i} \in A} \rotfv{v}{ S^{\prime}_{i} \cup M(S^{\prime}_{i}) } 
  \end{split}
\end{equation*}
Clearly the support property holds and the $\partial^{-}\tilde{z} = \partial^{-}\phi = \partial z$

Now for $d^{\prime}> 2$ and $g \in S$. Divide $\partial^{-}\phi = \partial^{-}\phi|_{gv} + \partial^{-}\phi|_{/ gv}$. Now let $\partial \phi^{*}$ the assignment obtained from $\partial^{-}\phi|_{gv}$ by removing from any face of it the vertex $gv$. By the induction assumption there is, $\tilde{z}^{*}$ that .., Consider now $\tilde{z}$ obtained by adding to each face of $\tilde{z}^{*}$ the vertex $gv$. 
Thus, 
\begin{equation*}
  \begin{split}
    \partial^{-} (\phi + \tilde{z}) &= \partial^{-}\phi|_{gv} + \partial^{-}\phi|_{/ gv} + \partial^{-}\tilde{z}|_{gv} + \partial^{-}\tilde{z}|_{/ gv} \\ 
    &= \partial^{-}\phi|_{/ gv}+ \partial^{-}\tilde{z}|_{/ gv}
  \end{split}
\end{equation*}
We continue until the $\partial z$ is spread in fewer than $d^{\prime}-1$ directions; then, clearly, we have that $\tilde{z}|_{/gv}$ is empty.




, So for the sum zeroing out, it follows that for any $S_{1} \in \{ S^{\prime}_{i}\}_{i}$ and any $g_{1} \in S_{1}$, there exist $S_{2} \in \{ S^{\prime}_{i}\}_{i}$ and $g_{2} \in S_{2}$ such that $S_{1}/g_{1} = S_{2}/g_{2}$. 




  Consider an assignment on $d^{\prime}-1$-faces $\xi \in \mathbb{F}_{2}^{\mathcal{F}_{d^{\prime-1}}}$ such $\xi$ is a boundary, it's  $\xi|_{v+} \neq 0$, and denote the vertices support of $\xi$ by $\mathbf{lk}_{0}( \xi |_{v+}) = A$. 


  Then there is an assignment $z$, rooted at $v$, such that $\mathbf{lk}_{0}(z|_{v}) $ is contained in $A$. 


  Notice that since $z$ is rooted at $v$ then $z|_{v} = z|_{v+}$. Denote by $k$ the number of generators supporting $ z|_{v}$. Namely, for $S^{\prime}_{1},..,S^{\prime}_{l}$, such that:
  \begin{equation*}
    \begin{split}
      z|_{v} =  \sum_{i}^{l} \rotfv{v}{S^{\prime}_{i}} 
    \end{split}
  \end{equation*}
  the number of supporting generators is $ k = | \cup_{i}S^{\prime}_{i}|$. We prove by induction on $k$. For the base, $k=d^{\prime}$, we have that  $z$ spreads on a single face. And 

\end{proof}


A full decoding cycle is composed of first applying the sweep-rule regarding the $X$-syndrome, then moving into the dual code (and reordering the qubits) by $\mathbf{H}_{\phi}$, and then applying the sweep-rule again with the change that the positive direction is flipped to be the negative direction. At the end, we return to the original code by applying $\mathbf{H}_{\phi}$.

\begin{definition}[Sweep cycle]
We define the \textit{sweep cycle} to be the following procedure:
\begin{enumerate}
    \item For each vertex, perform the sweep rule.
    \item Apply the Hadamard transform $\mathbf{H}^{\otimes n}$ followed by the $\phi$-transpose, and flip the positive direction.
    \item Again, apply the sweep rule at each vertex.
    \item Reverse stage 2 by reordering the qubits according to the $\phi$-transpose, and then apply the Hadamard transform $\mathbf{H}^{\otimes n}$. Flip the positive direction again.
\end{enumerate}
\end{definition}


To have our constructions yield circuits that have $\infty$-extensions \Cref{def:extension}, the sweep-rule cycles have to be 'decoding gadgets' \Cref{def:decgate}.  
\begin{claim}
  The local gates that perform the sweep rule are decoding gadgets.
\end{claim}
\begin{proof}
  A local sweep update acts on a constant-size neighborhood around a vertex, uses only local syndrome information, and may be implemented with fresh ancillas that are reset after the update. Any dependence on faults inside the gadget can therefore be pushed onto the data wires as an effective Pauli operator supported on the encoded register, while the reset ancillas carry no residual information. This is exactly the form required in the definition of a decoding gadget.
\end{proof}

Next, we prove that the sweep rule shrinks error subsets.
